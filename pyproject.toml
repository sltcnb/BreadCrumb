[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "breadcrumb"
version = "0.1.0"
description = "Signature-based file carver for disk images and block devices (photorec-style)"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE"]
keywords = ["forensics", "file-carving", "data-recovery", "disk-image", "photorec", "undelete"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Information Technology",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Security",
    "Topic :: System :: Recovery Tools",
]

[project.urls]
Homepage = "https://github.com/sltcnb/BreadCrumb"
Repository = "https://github.com/sltcnb/BreadCrumb"
Issues = "https://github.com/sltcnb/BreadCrumb/issues"

[project.optional-dependencies]
# Optional accelerator. BreadCrumb decrypts BitLocker with zero deps (pure-Python
# AES); installing cryptography swaps in C-backed AES for a large speedup.
bitlocker = ["cryptography>=3.0"]
# Wider EWF/E01 coverage. The built-in reader handles stored and deflate
# chunks and segmented sets; libewf's bindings (module `pyewf`) add the
# variants it does not model, notably Ex01/EWF2 and bzip2 chunks.
ewf = ["libewf-python>=20240506"]

[project.scripts]
bcrumb = "breadcrumb.cli:main"

[tool.setuptools.packages.find]
include = ["breadcrumb*"]

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.pytest.ini_options]
testpaths = ["tests"]

